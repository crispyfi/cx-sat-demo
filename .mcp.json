{
  "mcpServers": {
    "aoscx-vsx": {
      "command": "uv",
      "args": ["run", "python", "mcp_server.py"],
      "env": {
        "CX_USERNAME": "${CX_USERNAME}",
        "CX_PASSWORD": "${CX_PASSWORD}",
        "CX_API_VERSION": "${CX_API_VERSION:-v10.16}"
      }
    }
  }
}
